package com.example.recyclerview

class Product(val img: Int, val name: String, val number: String) {
    // Constructor parameters are declared in the primary constructor itself.
    // No need for a separate constructor function.
}
